import { BackgroundLayerConfiguration } from "./backgroundLayerConfiguration";

export class EndlessBackgroundConfiguration
{
  /**
   * The layers that compund the endless background.
   */
  public layers: Array<BackgroundLayerConfiguration>;
}